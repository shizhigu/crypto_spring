server.port=8888
period=60000000

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://crypto.ctsb6qhqivyr.us-west-2.rds.amazonaws.com:3306/crypto?useSSL=false
spring.datasource.username = gushizhi
spring.datasource.password = Cryptocrypto123

# Redis configuration
spring.data.redis.host=${app.redis.url}
spring.data.redis.port=6379

## Hibernate Properties
#The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

logging.level.org.hibernate.stat=debug
# Show all queries
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type=trace

mybatis.configuration.map-underscore-to-camel-case=true

# Binance URLs
KlineApi=https://api.binance.us/api/v3/klines?symbol=%s&interval=1m&startTime=%s&endTime=%s&limit=1000
TradeApi=https://api.binance.us/api/v3/trades?symbol=%s&limit=5